# I2 — Dependency and Synchronization

Determine dependency order and overlaps.

Compare candidates with current `main`. Rebase/merge only in controlled branches. Material synchronization changes require renewed evidence.
