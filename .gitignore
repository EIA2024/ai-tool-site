# Prompt-driven workflow runtime state
.workflow/runtime/
.workflow/**/*.tmp
.workflow/**/*.log

# Optional sibling worktree root when configured inside the repository
.workflow-worktrees/

# Common local artifacts
.DS_Store
Thumbs.db
